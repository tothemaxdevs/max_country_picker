class MaxCountry {
  final String? name, dialCode, code, flagEmoji, flagIconCircle, flagIconSquare;

  MaxCountry(
      {this.name,
      this.dialCode,
      this.code,
      this.flagEmoji,
      this.flagIconCircle,
      this.flagIconSquare});
}
